[project]
name = "baggiponte-blog"
version = "0.1.0"
description = "Tooling for marimo-backed blog content."
requires-python = ">=3.11"
dependencies = [
    "marimo>=0.19.11",
    "numpy>=2.3.5",
    "torch>=2.9.1",
]

[tool.uv]
package = false
